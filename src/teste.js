const name = "Jeronimo";

const person = {
    first: name,
    Plast: lastname,
};

console.log(person);

const sayHeloLinting = (fName) => {
    console.log(`Hello linting, ${fName}`);
};

sayHeloLinting("Jeronimo");
